<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ReportMapper">


    <select id="countAmount" resultType="java.math.BigDecimal">
        select count(amount) from orders where delivery_time
        between #{beginTime} and #{endTime} and status=#{status}


    </select>
    <select id="selectByTime" resultType="com.sky.entity.Orders">
        select * from orders where delivery_time between #{beginTime} and #{endTime}

    </select>
</mapper>
